# S3 bucket name (required for pipeline and DuckDB queries)
BUCKET_NAME=

# Pipeline generator config (all optional — defaults shown)
NUM_USERS=100
ACCOUNTS_PER_USER=10
TX_PER_ACCOUNT=85
SEED=42
CHURN_RATE=0

# Full-scale example (uncomment to run at ~20M rows)
# TX_PER_ACCOUNT=850
