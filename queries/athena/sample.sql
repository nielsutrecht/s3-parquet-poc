-- Sample Athena queries against the transactions table.
-- Table is defined in the Glue catalog; partitioned by year/month.
-- Queries will be added in the Athena implementation issue.

-- Placeholder: count all rows
SELECT COUNT(*) AS total_transactions
FROM transactions;
